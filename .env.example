# Server-only Gemini API credentials. Never prefix these with NEXT_PUBLIC_.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Public canonical production origin, without a trailing slash.
NEXT_PUBLIC_SITE_URL=https://your-domain.example
